<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>Wantok Keycloak SSO</display-name>

    <!-- =========================================================
         OFBiz web application context
         ========================================================= -->

    <context-param>
        <param-name>webSiteId</param-name>
        <param-value>WebStore</param-value>
    </context-param>

    <context-param>
        <param-name>localDispatcherName</param-name>
        <param-value>wankeycloak</param-value>
    </context-param>

    <context-param>
        <param-name>entityDelegatorName</param-name>
        <param-value>default</param-value>
    </context-param>

    <!--
        REQUIRED:
        Initializes OFBiz delegator, dispatcher, security and
        tenant-aware request context for this web application.
    -->
    <filter>
        <filter-name>ContextFilter</filter-name>
        <filter-class>org.apache.ofbiz.webapp.control.ContextFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>ContextFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Standard OFBiz session lifecycle listeners -->
    <listener>
        <listener-class>org.apache.ofbiz.webapp.control.ControlEventListener</listener-class>
    </listener>

    <listener>
        <listener-class>org.apache.ofbiz.webapp.control.LoginEventListener</listener-class>
    </listener>

    <!-- Main OFBiz controller -->
    <servlet>
        <servlet-name>ControlServlet</servlet-name>
        <servlet-class>org.apache.ofbiz.webapp.control.ControlServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>ControlServlet</servlet-name>
        <url-pattern>/control/*</url-pattern>
    </servlet-mapping>

    <session-config>
        <session-timeout>60</session-timeout>
    </session-config>

</web-app>
